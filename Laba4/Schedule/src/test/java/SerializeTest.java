
import org.testng.annotations.Test;
import org.testng.Assert;
import org.testng.annotations.DataProvider;
import model.*;
public class SerializeTest {
    //@Test(dataProvider = "JsonProvider")
   // public void testIntegerNumbersTask(Lesson num1, int num2) {
        //Assert.assertEquals(new Variant8().integerTask(num1), num2);
    //}

 /*   @DataProvider
    public Object[][] JsonProvider() {
        var lesson1= new Lesson.Builder()
                .withNumLesson(5)
                .withAuditory("25к 12 корп.")
                .withName("Мат.аналіз")
                .withNumberWeek(1)
                .withTeacher("Дорош Марія Михайлівна")
                .withType("практ.")
                .build();
        var lesson2= new Lesson().Builder()
                .withNumLesson(5)
                .withAuditory("312к 8 корп.")
                .withName("OpenGL")
                .withNumberWeek(1)
                .withTeacher("Павлов Іван Миколайович")
                .withType("Практ.")
                .build();
        //var json=
        return new Object[][]{{lesson1,},lesson2};


    }
    */

}
